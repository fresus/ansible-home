- name: install python packages
  apt: name={{ item }}
  sudo: yes
  with_items:
    - python-dev
    - python-pip
    - python3-dev
    - python3-pip

- name: update pip 
  pip: name=pip state=latest executable=/usr/bin/{{ item }}
  sudo: yes
  with_items:
    - pip
    - pip3

#- name: install python 3 pypi packages
#  pip: name={{ item }} executable=/usr/local/bin/pip3
#  sudo: yes
#  with_items:
#    - virtualenv
#    - virtualenvwrapper
