- name: config weechat repo
  apt_repository: repo="ppa:nesthib/weechat-stable"
  become: yes

- name: install weechat package
  apt: name={{ item }} update_cache=yes
  become: yes
  with_items:
    - weechat-curses
    - weechat-plugins
