dotfiles_git: git@github.com:fresus/dotfiles.git
dotfiles_path: "{{ lookup('env', 'HOME') }}/.dotfiles"
stow_program: xstow
install_packages:
  server:
    - htop
    - curl
    - mtr-tiny
    - pv
    - sysstat
  programing:
    - shellcheck
    - fonts-powerline
    - fonts-firacode
  desktop:
    - whois
    - pwgen
    - keepassx
    - adb
